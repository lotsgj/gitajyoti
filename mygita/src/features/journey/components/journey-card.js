// @ts-check
import { escapeHtml } from "../../../core/dom.js";
import { illustrationPlaceholder } from "../../../core/ui/illustration-placeholder.js";
export function journeyCard(journey,completed,experience){const ids=experience.activityIds;const done=ids.filter(id=>completed.includes(id)).length;const next=ids.find(id=>!completed.includes(id))||ids[ids.length-1];return `<article class="journey-card">${illustrationPlaceholder(experience.illustration,experience.title)}<div><div class="cluster"><span class="pill">${done} of ${ids.length} complete</span></div><h2>${escapeHtml(experience.title)}</h2><p>${escapeHtml(experience.subtitle)}</p><div class="progress" role="progressbar" aria-label="Journey progress" aria-valuemin="0" aria-valuemax="${ids.length}" aria-valuenow="${done}"><span style="width:${ids.length?done/ids.length*100:0}%"></span></div><a class="button button--primary" href="#/activity/${next}">${done===ids.length?"Review journey":"Continue next activity"}</a></div></article>`;}

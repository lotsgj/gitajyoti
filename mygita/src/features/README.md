# Feature modules

Each feature owns its page rendering, service, fixture repository, API repository, and feature-specific stylesheet. Features are introduced during Phase 1 and the subsequent vertical slices; shared infrastructure must remain in `core`, `shell`, or `ui`.

// @ts-check
import { escapeHtml } from "../core/dom.js";
import { initialsAvatar } from "../core/ui/initials-avatar.js";
import { identityRepository } from "../features/identity/index.js";
export async function profilePage(){const user=await identityRepository.getCurrentUser();if(!user)return `<section class="status-view panel"><h1>Sign in to open your profile.</h1><div class="status-view__actions"><a class="button button--primary" href="#/auth">Sign in</a></div></section>`;const p=user.personalDetails;return `<section class="profile-page"><header>${initialsAvatar(p.displayName||p.fullName,"lg")}<div><p class="eyebrow">Gita Sadhak</p><h1>${escapeHtml(p.displayName)}</h1><p>Truth alone liberates</p></div></header><form class="form-grid panel" data-form="profile"><label class="field"><span>Full name</span><input name="fullName" value="${escapeHtml(p.fullName)}" required></label><label class="field"><span>Display name</span><input name="displayName" value="${escapeHtml(p.displayName)}" required></label><label class="field"><span>Date of birth</span><input name="dateOfBirth" type="date" value="${escapeHtml(p.dateOfBirth)}" required></label><label class="field"><span>Email <small>(optional)</small></span><input name="email" type="email" value="${escapeHtml(p.email||"")}"></label><button class="button button--primary" type="submit">Save profile</button><p class="inline-notice" data-profile-notice hidden>Profile updated.</p></form></section>`;}

// @ts-check
export function authMethodsPage(){return `<section class="auth-page"><div class="dipa" aria-hidden="true"><span>दी</span><i></i></div><div class="auth-panel panel"><p class="eyebrow">Welcome Atithi</p><h1>Begin your My Gita journey</h1><p>Choose how you would like to continue.</p><div class="auth-methods"><a class="auth-method auth-method--active" href="#/auth/mobile"><strong>Mobile + OTP</strong><span>Continue with your Indian mobile number</span></a>${["Google","Microsoft","Custom email"].map(method=>`<button class="auth-method" disabled><strong>${method}</strong><span>Coming later</span></button>`).join("")}</div></div></section>`;}
export function mobilePage(){return `<section class="auth-step panel"><p class="eyebrow">Sign up or sign in</p><h1>Your mobile number</h1><p>We’ll send a one-time password. The country code is fixed for this prototype.</p><form class="form-grid" data-form="mobile"><div class="mobile-field"><span aria-label="Country code">+91</span><label class="visually-hidden" for="mobile">Mobile number</label><input id="mobile" name="mobile" inputmode="numeric" pattern="[6-9][0-9]{9}" maxlength="10" placeholder="98765 43210" required></div><p class="form-error" data-form-error hidden></p><button class="button button--primary" type="submit">Send OTP</button></form></section>`;}
export function otpPage(mobile=""){return `<section class="auth-step panel"><p class="eyebrow">Check your messages</p><h1>Enter the 6-digit OTP</h1><p>Sent to +91 ${mobile||"your mobile"}. For this prototype, use <strong>123456</strong>.</p><form class="form-grid" data-form="otp"><label class="field"><span class="field__label">One-time password</span><input name="otp" inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" maxlength="6" placeholder="123456" required></label><p class="form-error" data-form-error hidden></p><button class="button button--primary" type="submit">Verify and continue</button></form></section>`;}
export function onboardingPage(profile={}){return `<section class="auth-step panel"><p class="eyebrow">A little about you</p><h1>Complete your profile</h1><p>These details help us welcome you and shape your learning journey.</p><form class="form-grid" data-form="onboarding"><label class="field"><span>Full name</span><input name="fullName" value="${profile.fullName||""}" autocomplete="name" required></label><label class="field"><span>Display name</span><input name="displayName" value="${profile.displayName||""}" required></label><label class="field"><span>Date of birth</span><input name="dateOfBirth" type="date" value="${profile.dateOfBirth||""}" required></label><p class="form-error" data-form-error hidden></p><button class="button button--primary" type="submit">Enter My Gita</button></form></section>`;}

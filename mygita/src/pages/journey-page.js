// @ts-check
import { experienceRepository } from "../features/experience/index.js";
import { identityRepository } from "../features/identity/index.js";
import { journeyCard, journeyRepository } from "../features/journey/index.js";

export async function journeyPage(){const [user,state,experiences]=await Promise.all([identityRepository.getCurrentUser(),journeyRepository.getState(),experienceRepository.listExperiences()]);if(!user)return `<section class="status-view panel"><p class="eyebrow">Gita Sadhak</p><h1>Sign in to see your Journey</h1><p>Your enrolled experiences and progress will be gathered here.</p><div class="status-view__actions"><a class="button button--primary" href="#/auth">Sign up / Sign in</a></div></section>`;if(!state.journeys.length)return `<section class="status-view panel"><p class="eyebrow">My Journey</p><h1>Your path is ready to begin.</h1><p>Choose an experience and it will appear here with your next activity.</p><div class="status-view__actions"><a class="button button--primary" href="#/discover">Discover experiences</a></div></section>`;return `<section><header class="journey-header"><p class="eyebrow">Truth alone liberates</p><h1>My Journey</h1><p>Continue from where your inquiry last rested.</p></header><div class="journey-list">${state.journeys.map(journey=>journeyCard(journey,state.completed,experiences.find(item=>item.id===journey.experienceId))).join("")}</div></section>`;}

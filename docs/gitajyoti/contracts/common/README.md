# Common product integration concepts

This area will define reusable learner-reference, enrolment-reference, launch, progress-summary, completion-status, error, correlation, and contract-version concepts.

These concepts provide consistency; they do not create one generic API that every product must implement identically.

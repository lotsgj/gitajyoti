# MyGita product integrations

MyGita integrates with learning products through `mygita.api` and product-specific APIs. Integration concerns include enrolment provisioning, identity exchange, secure launch, return navigation, progress synchronization, completion, availability, and error handling.

MyGita's server—not the browser—owns product credentials and trust decisions.

# Landing region

The Landing region is the public entry to the Gita Jyoti site. It explains the purpose, learning philosophy, lineage, and invitation, then routes visitors to the MyGita region through relative `mygita/` links.

The implementation is the repository-root `index.html`, `style/`, and `assets/` content.
